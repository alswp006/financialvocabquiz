// Placeholder stub — NOT the real implementation.
// Written by the Tester agent only to satisfy `tsc --noEmit` for packet 0010's
// TDD red phase (src/__tests__/packet-0010.test.ts imports this module).
// The Coder agent replaces this with the actual page per .ai-factory/spec.md.
export default function Result() {
  return null;
}
