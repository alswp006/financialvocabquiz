// Leftover debug probe: sandbox `rm` on this file is blocked in this session so it
// could not be deleted. Unused — not imported anywhere. Safe to delete manually.
export function scratchHello(): string {
  return "hello";
}
