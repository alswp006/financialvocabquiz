#!/bin/bash
# Scratch repro script from investigating a require()/vi.mock incompatibility
# (see packet 0009 commit history). No longer needed — kept as a no-op
# because this sandbox cannot delete files.
exit 0
